# Review: 2026-06-05

> 模式: audit
> 触发: 手动
> 文件: 29
> 维度: html-structure, js-correctness, arch-check, rust-correctness, css-stability, rust-security, dead-code, rust-perf

## Js correctness

- ⚠️ [warning] d:\code\hello_world\src\js\event-delegation.js:262 — 截图面板事件委托注册在 #panel-screenshot 上，但 lightbox（#ssLightbox）和 dialog（#ssAddDialog）追加到 document.body，不在 #panel-screenshot 内。data-action 为 ss-lb-close/ss-lb-prev/ss-lb-next/ss-close-dialog/ss-pick-folder 的处理均为死代码
  - 建议: 将截图相关的键盘和点击事件委托改为注册在 document 上，或将 lightbox/dialog 追加到 #panel-screenshot 内。
- ⚠️ [warning] d:\code\hello_world\src\js\panel-screenshot.js:356 — 全局变量 window._ssDetectedSources 使用了 window._ 前缀而非项目约定的 window.__ 命名空间，可能与其他脚本冲突
  - 建议: 改为 window.__ssDetectedSources，并同步更新所有引用处（L379 等）。
- ⚠️ [warning] d:\code\hello_world\src\js\event-delegation.js:32 — gameTabs 点击事件处理函数是 async 但没有 try/catch，任何 await 失败都会成为未捕获的 Promise rejection
  - 建议: 将所有 async 事件处理函数包裹在 try/catch 中，或给返回的 Promise 添加 .catch()。
- ⚠️ [warning] d:\code\hello_world\src\js\event-delegation.js:106 — slotTabs 点击事件处理函数是 async 但没有 try/catch，任何 await 失败都会成为未捕获的 Promise rejection
  - 建议: 添加 try/catch 或 .catch()。
- ⚠️ [warning] d:\code\hello_world\src\js\event-delegation.js:165 — fileTagsContainer 点击事件处理函数是 async 但没有 try/catch，任何 await 失败都会成为未捕获的 Promise rejection
  - 建议: 添加 try/catch 或 .catch()。
- ⚠️ [warning] d:\code\hello_world\src\js\event-delegation.js:211 — backupList 点击事件处理函数是 async 但没有 try/catch，任何 await 失败都会成为未捕获的 Promise rejection
  - 建议: 添加 try/catch 或 .catch()。
- ⚠️ [warning] d:\code\hello_world\src\js\panel-convert.js:232 — addTimezoneBtn.addEventListener('click', async ...) — async 事件处理函数没有 try/catch，await 失败会产生未捕获的 Promise rejection
  - 建议: 用 try/catch 包裹函数体。
- ⚠️ [warning] d:\code\hello_world\src\js\panel-convert.js:213 — timezoneSets.addEventListener('change', async ...) — async 事件处理函数没有 try/catch
  - 建议: 用 try/catch 包裹函数体。
- ⚠️ [warning] d:\code\hello_world\src\js\panel-convert.js:131 — timezoneSets.addEventListener('click', async ...) — async 事件处理函数没有 try/catch，多个 await 调用可能产生未捕获的 rejection
  - 建议: 用 try/catch 包裹整个函数体。
- ⚠️ [warning] d:\code\hello_world\src\js\panel-backup.js:228 — browseFileBtn.addEventListener('click', async ...) — async 事件处理函数没有 try/catch
  - 建议: 用 try/catch 包裹函数体。
- ⚠️ [warning] d:\code\hello_world\src\js\panel-backup.js:243 — rehashBtn.addEventListener('click', async ...) — async 事件处理函数没有 try/catch
  - 建议: 用 try/catch 包裹函数体。
- ⚠️ [warning] d:\code\hello_world\src\js\panel-backup.js:255 — saveBackupBtn.addEventListener('click', async ...) — async 事件处理函数没有 try/catch
  - 建议: 当前已有 try/catch（L267），检查确认 catch 能覆盖所有 await。
- ⚠️ [warning] d:\code\hello_world\src\js\panel-settings.js:96 — settingsSetDirBtn.addEventListener('click', async ...) — async 事件处理函数没有 try/catch
  - 建议: 用 try/catch 包裹函数体。
- ⚠️ [warning] d:\code\hello_world\src\js\panel-settings.js:105 — settingsOpenDirBtn.addEventListener('click', async ...) — async 事件处理函数没有 try/catch
  - 建议: 用 try/catch 包裹函数体。
- ℹ️ [info] d:\code\hello_world\src\js\panel-backup.js:293 — refreshBackupList 使用 _backupListLock 防重叠锁，但 _backupListLock 在 finally 中释放（L362 正确），确认无误
  - 建议: 无 — 此实现正确。
- ℹ️ [info] d:\code\hello_world\src\init.js:22 — bindLogPanelEvents()（L22）在 setupEventDelegation()（L27）之前被调用，不符合事件委托集中化的原则
  - 建议: 将 bindLogPanelEvents 中的事件监听器迁移到 setupEventDelegation 中，或至少保证两者顺序一致。
- ❌ [error] d:\code\hello_world\src\js\panel-screenshot.js:378 — 函数 addDetectedSources(dialog) 被无参调用时 dialog 为 undefined，掉用 dialog.querySelectorAll 会抛出 TypeError
  - 建议: 将 event-delegation.js 第 347 行 addDetectedSources() 改为 addDetectedSources(dialog) 并传入正确的 dialog 引用。或者让此函数在没有 dialog 时通过 document.getElementById('ssAddDialog') 查找元素。
- ❌ [error] d:\code\hello_world\src\js\panel-screenshot.js:143 — loadThumbnails 中 invoke('get_screenshot_base64_batch') 没有 .catch()，IPC 失败时产生未捕获的 Promise rejection
  - 建议: 添加 .catch() 处理，例如 .catch(function(err) { window.__log.error('Screenshot', '缩略图加载失败: ' + err); });
- ❌ [error] d:\code\hello_world\src\js\panel-screenshot.js:243 — openLightbox 中 invoke('get_screenshot_base64_batch') 没有 .catch()，IPC 失败时产生未捕获的 Promise rejection
  - 建议: 添加 .catch() 处理。
- ❌ [error] d:\code\hello_world\src\js\panel-screenshot.js:399 — refreshScreenshotConfig 中 invoke('get_config') 没有 .catch()，IPC 失败时产生未捕获的 Promise rejection
  - 建议: 添加 .catch()，例如 .catch(function(err) { window.__log.error('Screenshot', '刷新配置失败: ' + err); });
- ❌ [error] d:\code\hello_world\src\js\panel-screenshot.js:391 — addDetectedSources 中 Promise.all(promises).then(...) 没有 .catch()，任一 add_screenshot_source 失败时产生未捕获的 Promise rejection
  - 建议: 添加 .catch() 处理。
- ❌ [error] d:\code\hello_world\src\js\panel-screenshot.js:313 — openAddSourceDialog 中 invoke('pick_directory').then(...) 没有 .catch()，IPC 失败时产生未捕获的 Promise rejection
  - 建议: 添加 .catch() 处理。
- ❌ [error] d:\code\hello_world\src\js\panel-screenshot.js:319 — openAddSourceDialog 中 invoke('add_screenshot_source').then(...) 没有 .catch()，IPC 失败时产生未捕获的 Promise rejection
  - 建议: 添加 .catch() 处理。
- ❌ [error] d:\code\hello_world\src\js\event-delegation.js:347 — data-action='ss-add-detected' 处理函数调用了 addDetectedSources() 不传 dialog，内部 dialog.querySelectorAll 会抛出 TypeError。且该按钮实际使用直接监听器，此路径是死代码
  - 建议: 将事件委托中的 ss-add-detected 处理删除，或者让按钮添加 data-action 属性并通过委托统一处理（需传入正确的 dialog 引用）。

## Arch check

- ⚠️ [warning] D:\code\hello_world\src\js\panel-convert.js:232 — addTimezoneBtn 使用 addEventListener 直接绑定点击事件，未走 event-delegation.js 的 data-action 委托模式
  - 建议: 在 addTimezoneBtn 上加 data-action="add-tz"，将处理逻辑移到 event-delegation.js 的 setupEventDelegation 中。
- ⚠️ [warning] D:\code\hello_world\src\js\panel-convert.js:242 — 窗口控制按钮（minimizeBtn/maximizeBtn/closeBtn）使用 addEventListener，未走事件委托
  - 建议: 在标题栏按钮上加 data-action="window-minimize" / "window-maximize" / "window-close"，统一在 event-delegation.js 处理。
- ⚠️ [warning] D:\code\hello_world\src\js\panel-backup.js:228 — browseFileBtn、rehashBtn、saveBackupBtn 使用 addEventListener 而非事件委托
  - 建议: 为这些按钮添加 data-action 属性，将逻辑迁移到 event-delegation.js。
- ⚠️ [warning] D:\code\hello_world\src\js\panel-backup.js:456 — showRestoreFileModal 在渲染函数中使用 addEventListener 绑定弹窗事件
  - 建议: 恢复文件选择弹窗使用 data-action 委托，或将弹窗静态放在 HTML 中通过类名控制显隐。
- ⚠️ [warning] D:\code\hello_world\src\js\panel-settings.js:5 — settingsBtn、themeSlider、autoStartToggle、reminderToggle、settingsSetDirBtn、settingsOpenDirBtn 等使用 addEventListener
  - 建议: 为所有设置控件添加 data-action，委托到 event-delegation.js。
- ⚠️ [warning] D:\code\hello_world\src\js\panel-settings.js:195 — openHolidayEditor 渲染函数中绑定多个 addEventListener（保存、复制、关闭等）
  - 建议: 将弹窗 HTML 模板化，通过 data-action 委托而非内联绑定。
- ⚠️ [warning] D:\code\hello_world\src\js\panel-settings.js:316 — holidayAddBtn 使用 addEventListener 直接绑定
  - 建议: 使用 data-action="add-holiday" 委托处理。
- ⚠️ [warning] D:\code\hello_world\src\js\panel-todo.js:257 — todoAddBtn 使用 addEventListener 直接绑定
  - 建议: 使用 data-action="add-todo" 委托处理。
- ⚠️ [warning] D:\code\hello_world\src\js\panel-todo.js:262 — todoSearch/todoFilterStatus/todoFilterPriority 使用 addEventListener
  - 建议: 通过 data-action="todo-search" / "todo-filter-status" / "todo-filter-priority" 委托处理。
- ⚠️ [warning] D:\code\hello_world\src\js\panel-todo.js:641 — openTodoEditModal 渲染函数中绑定大量 addEventListener（重复选择器、优先级、日期、提醒输入等 16 处）
  - 建议: 待办编辑弹窗用 data-action 委托或静态 HTML + 显隐控制。
- ⚠️ [warning] D:\code\hello_world\src\js\panel-notifications.js:113 — renderBanners 渲染函数中动态创建元素并绑定 addEventListener（关闭按钮、hover 暂停、汇总条点击等）
  - 建议: Toast 元素使用 data-action="dismiss-banner" + data-banner-id 委托处理。
- ⚠️ [warning] D:\code\hello_world\src\js\panel-screenshot.js:230 — openLightbox 渲染函数中绑定 addEventListener（关闭、上/下翻页、背景点击）
  - 建议: Lightbox 用 data-action 委托处理导航和关闭。
- ⚠️ [warning] D:\code\hello_world\src\js\panel-screenshot.js:310 — openAddSourceDialog 渲染函数中绑定 addEventListener（关闭/取消/浏览按钮/点击外部关闭等）
  - 建议: 添加来源弹窗用 data-action 委托。
- ⚠️ [warning] D:\code\hello_world\src\js\panel-log.js:166 — bindLogPanelEvents 使用 4 个 addEventListener 绑定日志控件
  - 建议: 将日志搜索/筛选/打开目录/清屏按钮改为 data-action 委托。
- ⚠️ [warning] D:\code\hello_world\src\init.js:49 — notificationBell 在 init.js 中使用 addEventListener 而非事件委托
  - 建议: 在 bell 上加 data-action="open-notification-center"，委托到 event-delegation.js。
- ℹ️ [info] D:\code\hello_world\src\js\panel-backup.js:67 — startInlineEditGame 和 startInlineEditSlot 也使用 addEventListener（keydown/blur） -- 但内联编辑本身需要即时拦截键盘事件，可以保留作为已知例外
  - 建议: 这个模式在编辑场景下是可以接受的例外，但建议统一用 data-action="inline-edit" 加全局委托抽象。
- ℹ️ [info] D:\code\hello_world\src\js\panel-convert.js:131 — timezoneSets.addEventListener('click'/'change') 虽然实现了基于 data-action 的委托，但绑定在 panel-convert.js 而非 event-delegation.js 中
  - 建议: 将这些委托统一迁移到 event-delegation.js 的 setupEventDelegation 中。
- ℹ️ [info] D:\code\hello_world\src\cmd\hash.rs:7 — compute_hash Tauri 命令对 file_paths 参数不做 sanitize_path_component 校验
  - 建议: 虽然哈希计算本身泄露风险较低，但与架构其他命令不一致，建议添加 canonicalize 校验。
- ℹ️ [info] D:\code\hello_world\src\cmd\backup.rs:50 — list_backups_internal 对 game_id 和 slot_id 没有内置路径校验，完全依赖调用方传入已 sanitize 的参数。
  - 建议: 在 list_backups_internal 入口处增加 assert 或安全检查，确保不会从外部绕过直接调用。
- ❌ [error] D:\code\hello_world\src\cmd\backup.rs:1 — cmd/backup.rs 包含大量本应属于 svc/ 层的业务逻辑
  - 建议: 将 list_backups_internal (line 50)、read_backup_meta (line 75)、find_backup_file (line 419) 等内部逻辑函数迁移到 svc/ 层，cmd/ 函数只做参数校验和 IPC 编排。
- ❌ [error] D:\code\hello_world\src\cmd\screenshot.rs:1 — cmd/screenshot.rs 包含大面积业务逻辑（VDF 解析、Steam/米哈游路径检测、Base64 LRU 缓存管理）
  - 建议: 将 Base64Cache 结构体及其方法（line 21-73）、VDF 解析器（line 181-249）、Steam 检测（line 253-313）、count_images_in_dir（line 282）等迁移到 svc/ 层，cmd/ 只保留 #[tauri::command] 函数。
- ❌ [error] D:\code\hello_world\src\cmd\hash.rs:33 — cmd/hash.rs 包含工具函数 compute_single_hash、compute_file_hash、compute_dir_hash、simple_glob_match 被 cmd/backup 跨模块引用，但纯业务工具应放在 svc/
  - 建议: 将 compute_single_hash、compute_file_hash、compute_dir_hash、simple_glob_match 移至 svc/ 模块，cmd/hash.rs 只保留 compute_hash Tauri 命令入口。
- ❌ [error] D:\code\hello_world\src\cmd\backup.rs:468 — open_folder Tauri 命令未对 path 参数做任何安全校验（sanitize_path_component 或 canonicalize）
  - 建议: 在 open_folder 入口处添加 path 校验：先 canonicalize() 确认路径在 backup_root 范围内，或至少调用 sanitize_path_component 拦截路径穿越。
- ❌ [error] D:\code\hello_world\src\svc\config_io.rs:8 — svc/config_io.rs 直接依赖 tauri::AppHandle 和 Tauri Manager API（app.path().app_data_dir()），违反 svc/ 层不得调用 Tauri API 的规则
  - 建议: 将 config_path、load_config、save_config 改造为接受 std::path::PathBuf 而非 AppHandle，在 cmd/ 或 main.rs 中先解析路径再传入 svc。

## Rust correctness

- ⚠️ [warning] D:\code\hello_world\src\cmd\backup.rs:315 — rename_backup 中 meta.json 写失败被 let _ 静默丢弃（同 create_backup 模式）
  - 建议: 与 line 224 相同修复：检查 std::fs::write 结果并返回错误
- ⚠️ [warning] D:\code\hello_world\src\cmd\backup.rs:449 — toggle_backup_pin 中 meta.json 写失败被 let _ 静默丢弃
  - 建议: 与 line 224 相同修复
- ⚠️ [warning] D:\code\hello_world\src\cmd\backup.rs:530 — recompute_backup_hash 中 meta.json 写失败被 let _ 静默丢弃（两处：line 530 和 line 555）
  - 建议: 与 line 224 相同修复
- ⚠️ [warning] D:\code\hello_world\src\main.rs:287 — reminder_thread 中 SystemTime::now().duration_since(UNIX_EPOCH).unwrap() 可能在系统时钟异常时 panic。该线程其余所有 I/O 错误都用 continue 优雅跳过，但此处 unwrap 不一致
  - 建议: 使用 unwrap_or 设置回退值：let nanos = std::time::SystemTime::now().duration_since(std::time::UNIX_EPOCH).unwrap_or_default().as_nanos();
- ⚠️ [warning] D:\code\hello_world\src\cmd\screenshot.rs:151 — 全局 Mutex<Base64Cache> 的 .lock().unwrap() 可能在监视线程 panic（poison）时导致命令 panic。line 151 和 line 165 两处相同模式
  - 建议: 使用 .lock().map_err(|e| e.to_string())? 或 .lock().unwrap_or_else(|e| e.into_inner()) 处理 poison。例如：let mut c = cache.lock().unwrap_or_else(|e| e.into_inner());
- ⚠️ [warning] D:\code\hello_world\src\cmd\screenshot.rs:399 — uuid_v4() 中 SystemTime::now().duration_since(UNIX_EPOCH).unwrap() 可能在系统时钟异常时 panic（与 main.rs:287 相同问题）
  - 建议: 使用 unwrap_or_default() 代替 unwrap()，或者直接使用 uuid crate 生成真正的 UUID v4
- ℹ️ [info] D:\code\hello_world\src\svc\config_io.rs:130 — save_config 返回 ()，内部写/重命名失败仅 log_error。所有调用方（set_config/save_holiday_data/add_timezone_set 等）都返回 OpResult { success: true, ... } 即使持久化实际失败
  - 建议: 将 save_config 改为返回 Result<(), String> 或 OpResult，调用方检查并传播错误。或者保持 () 但至少将调用方的 success 设为 false 当写入失败时
- ℹ️ [info] D:\code\hello_world\src\main.rs:162 — advance_daily_reminder 和 advance_monthly_reminder 中使用 .single().expect("Beijing has no DST")。对于 UTC+8 固定偏移确实安全，但如果 future 扩展增加 DST 时区参数就会 panic
  - 建议: 使用 .single()? (Option 传播) 代替 .single().expect()，保持函数返回 Option<i64> 的语义一致性
- ℹ️ [info] D:\code\hello_world\src\svc\tz.rs:44 — 多个 expect() 调用在日期计算函数中（nth_sunday_of_month line 44, 47; last_sunday_of_month line 53, 54; last_day_of_month line 60, 61）。当前调用栈已验证输入有效性，但新增调用路径可能触发 panic
  - 建议: 改为返回 Option 或 Result，将边界处理逻辑上移交给调用方
- ❌ [error] D:\code\hello_world\src\svc\config_io.rs:15 — config_path() 中 app_data_dir() 使用 expect() 直接 panic，该函数被所有配置读写路径（load_config/save_config）调用
  - 建议: 将 config_path 返回类型改为 Result<PathBuf, String>，在调用方用 ? 传播或降级处理。例如：pub fn config_path(app: &tauri::AppHandle) -> Result<PathBuf, String> { app.path().app_data_dir().map_err(|e| e.to_string()).map(|p| p.join("config.json")) }
- ❌ [error] D:\code\hello_world\src\cmd\backup.rs:224 — create_backup 中 meta.json 写失败被 let _ 静默丢弃。备份的 3 个步骤（复制文件/写meta/更新配置）中，meta写失败不返回任何错误，用户收到 success: true 但备份目录缺少 meta.json
  - 建议: 改 let _ 为 match 并返回 OpResult。例如: if let Err(e) = std::fs::write(backup_dir.join("meta.json"), &json) { return OpResult { success: false, message: format!("写入元数据失败: {}", e) } }

## Css stability

- ⚠️ [warning] D:\code\hello_world\src\css\panels.css:21 — 选择器 `.result` 为全局无作用域限定类，且未在 HTML/JS 中被引用
  - 建议: 移除未使用的 `.result` 规则，或添加父作用域限定
- ⚠️ [warning] D:\code\hello_world\src\css\panels.css:40 — 选择器 `.error` 和 `.success`（第 50 行）为全局无作用域类，易产生命名冲突
  - 建议: 添加父作用域限定，如 `.backup-panel .error`
- ⚠️ [warning] D:\code\hello_world\src\css\components.css:235 — 选择器 `.row` 过于通用，8 处 inline-style 覆盖其样式
  - 建议: 考虑更名为 `.flex-row` 或缩小作用域
- ℹ️ [info] D:\code\hello_world\src\css\panels.css:40 — `.result`（第 21 行）、`.error`、`.success` 三个全局选择器均未限定父级，可能被其他面板意外匹配
  - 建议: 建议统一添加面板作用域前缀
- ℹ️ [info] D:\code\hello_world\src\css\components.css:366 — `.btn-tiny` 定义从未在任何 HTML/JS 中被引用，属于死代码
  - 建议: 移除该规则
- ℹ️ [info] D:\code\hello_world\src\css\theme.css:20 — `--error-bg` 与 `--danger-bg` 在暗色/亮色模式下值完全相同
  - 建议: 合并为一个变量，移除冗余
- ℹ️ [info] D:\code\hello_world\src\css\theme.css:21 — `--error-text` 与 `--danger-text` 亮色模式下同为 `#dc2626`，暗色模式仅 0.04 差异
  - 建议: 评估是否可合并
- ℹ️ [info] D:\code\hello_world\src\css\theme.css:97 — `body.light` 中 `--radius-glass: 14px` 与 `:root` 默认值完全相同
  - 建议: 删除此重复声明
- ℹ️ [info] D:\code\hello_world\src\css\components.css:13 — `.panel-inner` 覆盖规则中 `height: 100%` 与基类第 10 行声明重复
  - 建议: 删除覆盖中的 `height: 100%`
- ℹ️ [info] D:\code\hello_world\src\css\layout.css:113 — `body.light .win-close:hover` 与第 111 行暗色规则完全相同
  - 建议: 删除 `body.light` 前缀，两个模式共享同一规则
- ℹ️ [info] D:\code\hello_world\src\css\components.css:203 — 基础 `button` 设置 `width: 100%` 导致 10+ 个按钮变体必须用 `width: auto` 覆盖
  - 建议: 考虑将默认 `width` 改为 `auto` 或移除默认值，让变体自行决定
- ℹ️ [info] D:\code\hello_world\src\css\panels.css:409 — `.todo-edit-body input/select/textarea` 与第 406 行 `.modal-body` 规则设置相同 `background: var(--surface)`
  - 建议: 删除 `.todo-edit-body` 规则，由 `.modal-body` 继承
- ❌ [error] D:\code\hello_world\src\css\panels.css:534 — `.toast-item` 背景色硬编码 `rgba(24, 28, 42, 0.95)`，未使用 CSS 变量
  - 建议: 提取为 `--toast-bg` 变量或在 `--bg` 基础上派生
- ❌ [error] D:\code\hello_world\src\css\panels.css:537 — `.toast-item` 边框硬编码 `rgba(255,255,255,0.08)`，应使用 `var(--glass-border)`
  - 建议: 替换为 `var(--glass-border)` 或 `var(--border)`
- ❌ [error] D:\code\hello_world\src\css\panels.css:539 — `.toast-item` 阴影硬编码 `rgba(0,0,0,0.3)`，未使用 `var(--shadow)`
  - 建议: 替换为 `var(--shadow)`
- ❌ [error] D:\code\hello_world\src\css\panels.css:556 — `.toast-info::before` 硬编码 `#4b8bf4`，应使用 `var(--accent)`
  - 建议: 替换为 `var(--accent)`
- ❌ [error] D:\code\hello_world\src\css\panels.css:557 — `.toast-success::before` 硬编码 `#34d399`，应使用 `var(--success-text)`
  - 建议: 替换为 `var(--success-text)`
- ❌ [error] D:\code\hello_world\src\css\panels.css:558 — `.toast-warning::before` 硬编码 `#fbbf24`，应使用 `var(--pin-color)`
  - 建议: 替换为 `var(--pin-color)`
- ❌ [error] D:\code\hello_world\src\css\panels.css:559 — `.toast-error::before` 硬编码 `#f87171`，应使用 `var(--error-text)`
  - 建议: 替换为 `var(--error-text)`
- ❌ [error] D:\code\hello_world\src\css\panels.css:566 — `.toast-source` 背景色硬编码 `rgba(75,139,244,0.15)`，应使用 `rgba(var(--accent-rgb), 0.15)`
  - 建议: 替换为 `rgba(var(--accent-rgb), 0.15)`
- ❌ [error] D:\code\hello_world\src\css\panels.css:587 — `.toast-close` 颜色硬编码 `rgba(255,255,255,0.3)`，未使用变量
  - 建议: 提取为 `--toast-close-color` 变量，并添加 light 模式覆盖
- ❌ [error] D:\code\hello_world\src\css\panels.css:655 — `.notif-center` 背景/边框/阴影全部硬编码，未使用 CSS 变量
  - 建议: 使用 `var(--glass-bg)`, `var(--glass-border)`, `var(--shadow)` 替代
- ❌ [error] D:\code\hello_world\src\css\panels.css:97 — `.backup-item.hash-match` 颜色硬编码 `#34d399` / `rgba(52,211,153,0.06)`
  - 建议: 提取为 `--hash-match-color` 变量或从 `--success-text` 派生
- ❌ [error] D:\code\hello_world\src\css\panels.css:100 — `.backup-item.hash-duplicate` 背景硬编码 `rgba(251,191,36,0.06)`，未基于 `--pin-color`
  - 建议: 替换为基于 `--pin-color` / `var(--pin-color-rgb)` 的表达式
- ❌ [error] D:\code\hello_world\src\css\panels.css:117 — `.hash-badge.match` 颜色硬编码 `#34d399` / `rgba(52,211,153,0.2)`
  - 建议: 使用与 `.hash-match` 一致的变量
- ❌ [error] D:\code\hello_world\src\css\panels.css:121 — `.hash-badge.duplicate` 背景硬编码 `rgba(251,191,36,0.2)`
  - 建议: 基于 `--pin-color` 推导背景色
- ❌ [error] D:\code\hello_world\src\css\components.css:339 — `.btn-danger:hover` 背景硬编码 `rgba(239,68,68,0.18)`，应使用 `var(--danger-bg)`
  - 建议: 替换为 `var(--danger-bg)` 或与其他危险状态保持一致
- ❌ [error] D:\code\hello_world\src\css\components.css:353 — `.btn-pin:hover` 背景硬编码 `rgba(251,191,36,0.1)`，应基于 `--pin-color`
  - 建议: 提取为 `--pin-bg` 变量或计算表达式
- ❌ [error] D:\code\hello_world\src\css\panels.css:861 — `.todo-priority.high` 文字颜色硬编码 `#000`
  - 建议: 使用 `var(--text)` 或基于 `--pin-color` 的可读颜色变量
- ❌ [error] D:\code\hello_world\src\css\panels.css:1362 — `.ss-card:hover` 阴影硬编码 `rgba(0,0,0,0.35)`，应使用 `var(--shadow)`
  - 建议: 替换为 `var(--shadow)`
- ❌ [error] D:\code\hello_world\src\css\panels.css:1389 — `.ss-card .ss-game-tag` 背景硬编码 `rgba(75,139,244,0.9)`，应使用 `rgba(var(--accent-rgb), 0.9)`
  - 建议: 替换为 `rgba(var(--accent-rgb), 0.9)`
- ❌ [error] D:\code\hello_world\src\css\panels.css:1426 — `.ss-lightbox` 背景硬编码 `rgba(0,0,0,0.88)`
  - 建议: 提取为 `--lightbox-bg` 变量
- ❌ [error] D:\code\hello_world\src\css\panels.css:1453 — `.ss-lb-image` 阴影硬编码，应使用 `var(--shadow)`
  - 建议: 替换为 `var(--shadow)`

## Rust security

- ⚠️ [warning] D:\code\hello_world\src\cmd\backup.rs:40 — sanitize_path_component 未检测空字节（\0）——在 Windows 上虽影响有限，但在跨平台场景下空字节可截断路径。
  - 建议: 增加 `name.contains('\0')` 的检查，与 .. / / \\ 并列。
- ⚠️ [warning] D:\code\hello_world\src\cmd\backup.rs:205 — 备份文件复制时 file_name 来自 Path::file_name()，理论上安全（file_name 只返回末段），但 file_name 为 None 时回退到 fp 原始值，存在风险。
  - 建议: 当 file_name() 返回 None 时拒绝操作而非回退到原始路径。
- ⚠️ [warning] D:\code\hello_world\src\cmd\backup.rs:197 — backup_root 从 AppConfig 读取且未做任何合法性校验，若攻击者可修改 config.json 可诱导备份到任意目录。
  - 建议: 在 load_config 后增加 backup_root 的合法性校验（是否绝对路径、是否在合理范围内）。
- ⚠️ [warning] D:\code\hello_world\src\cmd\backup.rs:171 — 去重检查（create_backup）中 folder_name 直接从现有备份列表中读取，列表来自文件系统，meta.json 被篡改可影响去重逻辑。
  - 建议: 在 read_backup_meta 中对 folder_name 调用 sanitize_path_component 验证。
- ℹ️ [info] D:\code\hello_world\src\cmd\backup.rs:50 — list_backups_internal 对 game_id 和 slot_id 没有内置路径校验，完全依赖调用方传入已 sanitize 的参数。
  - 建议: 在 list_backups_internal 入口处增加 assert 或安全检查，确保不会从外部绕过直接调用。
- ℹ️ [info] D:\code\hello_world\src\cmd\screenshot.rs:96 — scan_screenshots 存在 TOCTOU 竞争条件：canonicalize 验证后到 spawn_blocking 实际读文件之间，路径可被替换（Windows 上 symlink 攻击风险较低但仍存在）。
  - 建议: 在 spawn_blocking 内部再次 canonicalize 并验证路径仍在合法范围内。
- ❌ [error] D:\code\hello_world\src\cmd\screenshot.rs:141 — 任意文件读取：get_screenshot_base64_batch 的 paths 参数未经过任何路径校验或 canonicalize，任何前端代码可读取系统任意文件并以 base64 data URI 返回。
  - 建议: 对每个传入路径调用 std::fs::canonicalize()，然后检查 resolved 路径是否在已注册的截图来源路径之下（类似 scan_screenshots 的做法）。
- ❌ [error] D:\code\hello_world\src\cmd\backup.rs:125 — 路径遍历：create_backup 的 file_paths 参数未经 sanitize_path_component 校验，用户可传入包含 ../ 的路径从系统任意位置复制文件到备份目录。
  - 建议: 遍历 file_paths 时对每个路径调用 sanitize_path_component 检查文件名部分，或使用 canonicalize 确保路径在预期范围内。
- ❌ [error] D:\code\hello_world\src\cmd\hash.rs:9 — 任意文件读取：compute_hash 的 file_paths 参数未做任何路径合法性校验，攻击者可通过 hash 计算侧信道泄露任意文件内容。
  - 建议: 对每个 file_path 调用 std::fs::canonicalize() 并限制在备份目录或用户指定的合法路径范围内。
- ❌ [error] D:\code\hello_world\src\cmd\backup.rs:470 — 路径遍历：open_folder 的 path 参数未经任何 sanitize_path_component 或 canonicalize 校验，直接传给 explorer.exe，可打开系统任意目录。
  - 建议: 调用 std::fs::canonicalize() 验证路径，并限制其必须在 backup_root 或 app_data_dir 下。
- ❌ [error] D:\code\hello_world\src\cmd\backup.rs:349 — 路径遍历风险：restore_backup 中 selected_files 的文件名直接从 meta.json 读取后拼接至路径，若 meta.json 被篡改可导致路径穿越。
  - 建议: 对每个 selected file 的 name 字段调用 sanitize_path_component() 后再 join 路径，同时 verify 文件确实在 backup_dir 下。
- ❌ [error] D:\code\hello_world\src\cmd\backup.rs:351 — 路径遍历风险：restore_backup 中 original_path 直接从 meta.json 读取后用于 std::fs::copy 的目标路径，若 meta.json 被篡改可覆盖任意文件。
  - 建议: 对 original_path 使用 canonicalize 检查目标路径是否在预期恢复范围内（如 SlotConfig.file_paths 中的路径前缀）。
- ❌ [error] D:\code\hello_world\src\cmd\backup.rs:401 — 路径遍历风险：restore_backup 中 file.name 未经 sanitize_path_component 即用于 backup_dir.join(&file.name)，meta.json 中 name 含 ../ 可导致路径穿越。
  - 建议: 在构建 to_restore 时对每个 file.name 调用 sanitize_path_component()。
- ❌ [error] D:\code\hello_world\src\cmd\backup.rs:519 — 路径遍历风险：recompute_backup_hash 中 name 直接从 meta.json 读取后用于 backup_dir.join(name)，meta.json 被篡改时可导致路径穿越。
  - 建议: 对 name 调用 sanitize_path_component() 后再 join，或验证最终路径仍在 backup_dir 下。

## Dead code

- ⚠️ [warning] src/css/panels.css:876 — CSS 类 .todo-due 定义后未被任何 HTML/JS 模板引用，为死样式
  - 建议: 删除 .todo-due 和 .todo-due.overdue 两条规则
- ⚠️ [warning] src/css/panels.css:1020 — CSS 选择器 .todo-edit-actions 定义后未被任何元素使用
  - 建议: 删除 .todo-edit-actions button 规则
- ⚠️ [warning] src/css/panels.css:1043 — CSS 类 .todo-edit-row 定义后未被任何 HTML/JS 模板引用
  - 建议: 删除 .todo-edit-row 和 .todo-edit-row > * 两条规则
- ⚠️ [warning] src/css/panels.css:1562 — CSS 类 .ss-header 定义后未被任何 HTML/JS 模板引用，为死样式
  - 建议: 删除 .ss-header 及相关子选择器规则
- ℹ️ [info] src/core.js:164 — bindTabEvents 函数体为空，仅保留一条注释说明功能已迁移至 setupEventDelegation，但 renderTabBar 仍然调用它
  - 建议: 移除 renderTabBar 中对 bindTabEvents() 的调用，然后删除此空函数定义
- ❌ [error] src/js/panel-notifications.js:12 — 函数 pushNotification 定义后未被任何代码调用
  - 建议: 删除此函数定义，或将其改为内部函数仅在需要时导出
- ❌ [error] src/js/panel-notifications.js:46 — 函数 clearAllTimers 定义后未被任何代码调用
  - 建议: 删除此函数定义，相关清空逻辑已在 renderBanners 中通过遍历 _notificationTimers 处理

## 总结

- ❌ 严重: 46 | ⚠️ 警告: 46 | ℹ️ 信息: 20
